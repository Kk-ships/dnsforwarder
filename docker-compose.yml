services:
  app:
    build:
      context: .
      args:
        TARGETOS: linux
        TARGETARCH: amd64
    ports:
      - "53:53/udp"
    env_file:
      - .env
    restart: always
    environment:
      - TZ=Asia/Kolkata