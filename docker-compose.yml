services:
  app:
    build: .
    ports:
      - "53:53/udp"
    env_file:
      - .env
    restart: always
    environment:
      - TZ=Asia/Kolkata
    depends_on:
      - valkey

  valkey:
    image: valkey/valkey:alpine
    volumes:
      - valkey_data:/data
    restart: always

volumes:
  valkey_data:
